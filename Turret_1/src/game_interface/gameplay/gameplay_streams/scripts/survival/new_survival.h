#pragma once
#ifndef SURVIVAL_CREATE_PLAYER_BASE
#define SURVIVAL_CREATE_PLAYER_BASE

class World;
class PreSettings;

void createPlayerBase(World& world, const PreSettings& preSettings);

#endif // SURVIVAL_CREATE_PLAYER_BASE
